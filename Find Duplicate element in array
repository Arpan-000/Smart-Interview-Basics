#include <bits/stdc++.h>
using namespace std;

int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    
    int n,flag =0;
    scanf("%d",&n) ; 
    int a[n];
    for(int i = 0;i<n;i++) 
     { 
          scanf("%d",&a[i]); 
          for(int j=0;j<i;j++){ 
               if(a[j]==a[i]) 
                 {flag =1;  
                 break;}

          } 
     
          if(flag ==1)  {
          printf("%d",a[i]); 
            break;}
}

   
    return 0;
}
